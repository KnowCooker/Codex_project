# 开发交接记录

更新时间：2026-09-18

本文件用于在新的电脑或新的 Codex 任务中恢复项目上下文。继续开发前请同时阅读 `README.md` 和策划书 `噪声分析小程序项目策划书_V2.md`。

## 仓库与分支

- GitHub：`https://github.com/KnowCooker/Codex_project.git`
- 当前开发分支：`master`
- 小程序类型：原生微信小程序，无 npm 依赖
- iOS 测试 AppID：`wx4e4e4dd771fc9421`
- `project.config.json` 已配置上述 AppID
- GitHub `master` 是跨电脑继续开发的唯一源码基线；同步完成后应以 `git status --short --branch` 显示 `master...origin/master` 且工作区为空为准
- `project.private.config.json` 仅保存微信开发者工具的机器本地设置，已被 `.gitignore` 排除；新电脑导入项目后会重新生成
- 小程序沙盒中的录音/WAV、校准值、备注和标签不属于源码，不会通过 GitHub 迁移

## 已确定的测试环境

- 手机：iPhone 16 Pro
- iOS：26.6.1
- 微信：8.0.76
- 耳机：AirPods Pro 3，型号 A3065
- AirPods 固件：尚未记录
- 首轮开发仅针对 iOS；耳机输入在完成真机路由验证前保持“未验证”状态

## 当前实现状态

- 48 kHz 单通道 PCM 输入，连续 7 Hz 高通和 129 抽头 FIR 抗混叠滤波后降采样至 4 kHz，为 1 kHz 显示上限保留奈奎斯特余量。
- 2048 样本 Hann 数据窗及 2048 点 FFT；频点间隔与有效分辨率均约 1.95 Hz，数据窗约 512 ms。幅值按 Hann 窗相干增益归一化，已修正此前约 `-6.02 dB` 的系统性低估。
- 频谱横轴上下限均改为直接输入，可在 20–1000 Hz 内缩放。实时分析使用 2048 样本窗、683 样本帧移，重叠率约 66.65%；3 秒平均按 `floor((12000-2048)/683)+1` 固定取 15 帧。
- 实时曲线与单值指标使用功率域滑动平均：新帧到达时减去最老帧的功率并加入最新帧；时长可在 1–30 秒调整，增大时可从最长 30 秒的历史缓存重建当前和。
- FFT 计算频率约 5.86 帧/秒；图窗独立以最多 12.5 FPS 对相邻平均频谱做 dB 线性显示插值，插值不写回平均器，也不影响任何单值指标。固定纵轴时不再扫描频谱计算自动范围，无变化的计时数据不再重复 `setData`。
- 部分微信基础库会忽略 `readFile` 的 `position/length`。当前利用录音最长 30 秒（约 2.75 MiB）的边界，只调用一次无范围参数的 `readFile`，随后在内存中按 64 KB 协作分块并逐块让出事件循环。离线与实时分析共同引用 `services/analysis-config.js`：48→4 kHz、129 抽头 FIR、2048 点 Hann 窗、683 点帧移（约 2/3 重叠）、20–1000 Hz。
- 频谱默认固定纵轴为 `-80～-20 dB`，仍支持自动/手动纵轴及对数/线性横轴；计权、平均时长、坐标轴和参考操作默认折叠在图窗下方的“绘图设置”中。
- 频谱支持 A 计权和线性计权；单位分别显示为 dBA 和 dB。绘图设置新增全局手动校准修正（-100～100 dB），修正量同时作用于频谱与指标，固定纵轴上下限按修正差值同步平移；修正只影响显示，不修改原始 WAV。
- 20–60 Hz、70–150 Hz、160–240 Hz 三段半透明频带背景。
- 实时指标包括总声压级，以及上述低频、中频、空腔声三个频段的峰值。
- 可将当前频谱设为会话内参考；参考曲线为橙色虚线，并显示对应的四项参考指标。切换计权时实时和参考曲线及指标同步重算。
- 参考也可从“录音文件”弹窗导入；系统读取完整 WAV，以与实时分析一致的 512 ms 窗和约 2/3 重叠计算功率平均 FFT，计算期间暂停实时分析并显示进度。
- 分析与录音分离，用户单独开始/结束录音，每段录音最长 30 秒；录音中显示 0.1 秒计时与进度，结束后将原始 PCM 分块封装为持久 WAV 文件。
- 正常结束录音或录满 30 秒后停留在分析页、显示成功提示并自动恢复实时分析，不再跳转到记录详情；若用户明确结束分析或输入中断则不自动恢复。结束录音与结束分析按钮字号已统一。
- 新增独立“录音文件”标签页，显示 WAV/旧 PCM、WAV 采样率、时长、大小、采集模式并可进入详情回放；支持列表内重命名和左滑置顶/删除。
- 首页入口改为圆形“开始测试”按钮；底部改为自定义 TabBar，文字放大到 28rpx，导航为“首页、录音文件、数据对比”。原“记录”页及录音页右上角重复入口已移除。
- iOS 耳机模式不使用 `auto`，而是直接请求 `headsetMic`。已移除进入页面时的 `getAvailableAudioSources` 查询及每 800 ms 路由轮询：该原生接口在部分 iOS/微信组合上可能阻塞 UI。当前依靠录音器错误、非预期暂停和音频中断停止测试，实际物理路由保持“未验证”。
- 采集启动新增递增操作令牌；`requesting/checking` 阶段也可被 `stop()` 取消，每个异步授权点之后都会验证令牌。分析页隐藏或销毁时无条件取消采集，避免页面退出后异步流程继续启动录音器；单个异常 PCM 帧限制为 128 KB 后再分析。
- 已通过固定缓冲区、FFT/Hann/旋转因子缓存、绘图请求合并和按屏幕像素合并绘图点降低延迟与垃圾回收卡顿。
- 暂停操作先更新 UI 并停止后续 PCM 计算；暂停和继续按钮有不同颜色。
- 分析页已解除 `disableScroll`，新增指标超出一屏时可以自然下滑。
- 录音详情不再执行 FFT，只显示源格式、时长、采样率、录音时间与录音时校准差值。数据对比按需读取完整 WAV 并重新计算整段功率平均 FFT，不复用实时测试结果；实时与离线横轴上限统一为 1 kHz。
- 录音记录只持久化原始时域 WAV 与采样率、声道数、位深、时长、路径、大小、录音开始时校准差值及名称/备注/标签等必要元数据；不保存频谱、FFT 参数或派生声级。对比时每条曲线分别使用自身记录的校准差值；旧记录缺少该字段时按 0 dB 处理。
- 记录详情支持本地重命名、备注和最多 8 个去重标签；旧记录缺少这些字段时可直接补充，无需迁移。

## 已完成的代码级验证

- 所有 JavaScript 文件通过 `node --check`。
- `app.json`、页面 JSON 与项目 JSON 均通过解析，`app.json` 中登记的页面文件完整存在。
- `git diff --check` 通过。
- FFT 复用缓冲区后连续 50 帧输出稳定。
- 40 Hz、100 Hz、200 Hz 测试信号峰值分别约为 39.06 Hz、99.61 Hz、199.22 Hz，均落在最近的 1.95 Hz FFT 频点。
- A 计权修正在 20 Hz、100 Hz、500 Hz、1000 Hz 分别约为 -50.39 dB、-19.14 dB、-3.25 dB、0 dB。
- Radix-2 FFT 已与直接 DFT 及 Parseval 能量关系交叉验证，单精度测试的最大数值误差小于 `6e-7`；非 2 次幂长度会拒绝计算。
- 0.5 幅值的 125、500、900.39、998.05 Hz 整频点单音经 Hann 窗后幅值误差小于 `0.000002 dB`，峰值频率误差为 0 个 bin。
- 48→4 kHz 抗混叠链实测响应约为：100 Hz `+0.01 dB`、500 Hz `+0.03 dB`、900 Hz `-0.02 dB`、1000 Hz `-0.18 dB`、3000 Hz `-63.78 dB`。
- 实时 3 秒平均器已用递增功率帧回归：目标帧数固定为 15，第 20 帧输出等于第 6–20 帧的功率算术平均；切换到 1 秒时固定为 3 帧，再切回可从保留历史正确重建。
- WAV 封装已通过 600123 字节、多分块及模拟部分写入测试，文件头和 PCM 负载完整。
- 文件平均 FFT 已用 4 秒、48 kHz、单通道、16 位的 900 Hz 合成 WAV 回归：降至 4 kHz 后按 2048/683 窗与帧移得到 21 个完整帧，主峰定位为 900.39 Hz，频谱最高点为 1000 Hz；进度序列从 1% 开始并以 100% 结束。
- 数据对比页首次选择录音卡在 1% 的根因是 `analysisToken` 未初始化：`++undefined` 得到 `NaN`，而 `NaN` 与自身不相等，导致全部回调和结果被丢弃。现已在 `onLoad` 初始化为 `0`，页面级回归确认进度到 100%、缓存写入并触发绘图。
- 离线 FFT 调用 `analyse(..., { spectrumOnly: true })`，跳过逐帧 RMS、数字峰值、削波和自动纵轴扫描；FIR 环形缓冲去掉逐抽头取模，并利用奇数长度低通滤波器的系数对称性将乘法次数约减半。优化前后数值最大误差回归小于 `6e-8`。
- iOS 路由选择回归确认：手机为 `buildInMic`，耳机为 `headsetMic`。
- 实时滑动平均已通过功率域数值、RMS 同步、1–30 秒边界、动态扩大窗口复用历史及 40 秒连续帧缓存上限回归。
- 录音记录已通过重命名、置顶、取消置顶、新记录保持在置顶项之后及删除持久化回归。
- 多记录对比已改为只接受可读 WAV，并在页面会话内缓存从完整文件重新计算的平均频谱；代码中已移除对 `record.summary.spectrum` 的读取。录音保存不跳转流程已回归。
- 旧记录迁移已验证：派生频谱、FFT 参数与平均时长字段会被移除，WAV 路径和 48 kHz 原始采样率保留，缺失的录音校准值补为 0 dB；线性/A 计权下校准均只应用一次。

## 尚需真机确认

- 微信开发者工具的 CLI 服务端口此前处于关闭状态，因此最新界面尚未完成 CLI 预览编译。路径：`设置 -> 安全设置 -> 服务端口`。
- 微信真机返回 PCM 的实际位深、字节序、采样率，以及 `audioSource` 在 iPhone 和 AirPods 下的真实路由。
- AirPods 断开事件、左右耳输入位置和固件版本。
- 暂停响应、频谱帧率、偶发卡顿、丢帧和发热情况。
- iPhone 与 AirPods 真机生成 WAV 后的回放、时长与文件完整性。
- 手动 dB 修正只是用户输入的统一偏移；在未使用可追溯标准声源或已校准声级计完成现场标定前，不应把当前 dB/dBA 数值用作合规声级计读数。
- 不能仅依据手机型号和公开灵敏度换算真实 SPL；后续若加入绝对声级，应采用同一设备、同一路由与标准声源/已校准声级计的现场偏移及频响校准，并保存校准版本与可信度。

## 发布进度

- 2026-09-13 已通过微信开发者工具成功上传开发版本 `1.0.0`。
- 上传说明：`公开测试初版：实时频谱、A/线性计权、参考曲线与本地录音`。
- GitHub `master` 已作为跨电脑继续开发的唯一代码基线；开始工作前必须先执行 `git pull --ff-only origin master` 并阅读本文件。
- 微信开发者工具中已上传的 `1.0.0` 早于本次 GitHub 同步，不包含 WAV 封装、录音文件页、文件平均 FFT 参考和最新耳机路由守护。需要用户另行指示后再上传新开发版本。
- 当前状态：本轮源码和交接文档已同步至 GitHub；尚未在微信公众平台配置麦克风隐私用途、提交审核和正式发布。
- 微信审核通过后仍需管理员在版本管理页面点击发布，并选择全量发布，任意微信用户才可访问正式版。

## 协作约定

- 普通修改只保存在本地。
- 只有用户明确要求“同步/更新 GitHub”时才提交并推送。
- 修改前先执行 `git status --short --branch`，避免覆盖用户尚未提交的改动。
- 修改后至少执行 JavaScript 语法检查和 `git diff --check`；涉及 DSP/计权时补做对应数值回归。

## 在另一台电脑恢复

新电脑需先安装 Git、Codex 和微信开发者工具。源码没有 npm 依赖，不需要执行 `npm install` 或额外构建命令。

```powershell
git clone https://github.com/KnowCooker/Codex_project.git "Mini noise"
Set-Location "Mini noise"
git switch master
git pull --ff-only origin master
git status --short --branch
```

然后在 Codex 中打开克隆得到的 `Mini noise` 文件夹，发送：

> 请先完整阅读 DEVELOPMENT_HANDOFF.md、README.md 和噪声分析小程序项目策划书_V2.md，检查 git status，然后基于当前 master 继续开发。除非我明确要求，否则不要提交或推送 GitHub。

最后在微信开发者工具中导入同一文件夹，确认 AppID 为 `wx4e4e4dd771fc9421`，登录有该 AppID 权限的微信账号，重新授予麦克风权限，并使用真机调试继续验证。开发者工具会在本机重新生成被 Git 忽略的 `project.private.config.json`。

GitHub 只恢复源码和文档，不恢复旧电脑微信小程序沙盒中的录音文件与本地存储。需要延续同一批测试数据时，应在换机前另行导出原始 WAV，并记录对应的校准差值、名称、备注和标签。

如果另一台电脑已经克隆过仓库，先确认没有未提交修改，再执行：

```powershell
git switch master
git pull --ff-only origin master
git status --short --branch
```

预期状态为 `master...origin/master` 且没有文件变更。可再执行 `git log -1 --oneline`，确认当前提交与 GitHub `master` 最新提交一致。若已有本地修改，不要强制覆盖，应先让 Codex 检查并妥善合并。
