# 噪声分析测试小程序项目策划书（V2.0）

版本日期：2026-09-12  
项目形态：iPhone 版微信小程序 + Android 版微信小程序  
文档状态：需求修订版

## 一、修订摘要

本版策划书以以下已确认条件为基础：

1. 分别建设两个独立微信小程序，分别服务于 iPhone 和 Android 手机。
2. 采集模式分为“手机麦克风”和“耳机麦克风”，两者严格互斥，任何时刻只允许一个输入源工作。
3. 全部实时分析采用单通道输入，不再考虑同时多通道或双耳同步分析。
4. 耳机模式下，在系统、耳机及微信能力允许时，可由用户选择左耳或右耳麦克风；无法确认时使用自动模式。
5. iOS 版可根据 iPhone/AirPods 型号提供“参考估算声压级”，不提供专业校准或计量承诺。
6. Android 版首期只提供 dBFS、相对幅值和频谱，不进行型号级声压校准。

## 二、项目结论

在取消双耳同步和多通道分析后，项目主体功能具备实施可行性：

- 手机单通道录音、实时 PCM 分片、FFT、波形和录音回放：可行性高。
- 手机和耳机模式互斥切换：业务逻辑可行，但无线耳机的真实音频路由仍受微信、操作系统和耳机实现影响。
- 耳机左/右侧选择：属于条件性功能。小程序不直接控制 AirPods 左右耳，只能调用系统提供的耳机输入并指导用户在系统或耳机配套应用中选择。
- iOS 参考估算声压级：技术上可实现，但结果只能标记为“参考估算”，数据库没有可靠数据时自动回退到 dBFS。
- Android 型号数量和音频实现差异较大，首期不进行声压级换算是合理选择。

项目不定位为专业声级计，不用于职业噪声合规、环境执法、设备计量验收或其他要求可追溯校准的场景。

## 三、项目目标

### 3.1 产品目标

建设一套轻量、便携的声音观察和相对频谱分析工具，帮助用户：

- 观察环境和设备噪声的频率组成；
- 查找主要频率和异常峰值；
- 对比同一设备、同一输入模式下的前后变化；
- 使用手机或无线耳机进行单通道采集；
- 录制、回放、命名和保存测试音频；
- 在 iOS 支持型号上查看参考估算声压级；
- 为后续声谱图、倍频程、统计声级和异响识别预留扩展能力。

### 3.2 非目标

首期明确不包括：

- 手机和耳机同时采集；
- 左右耳麦克风同步采集；
- 双通道或多通道 FFT；
- 双耳相位、相干性和声源定位；
- 专业校准和 IEC 61672 等级声明；
- 锁屏、后台无间断测量；
- 无缝生成超过微信单次录音上限的一个连续文件；
- 自动控制 AirPods 使用左耳或右耳麦克风。

## 四、产品定位与用户

### 4.1 产品定位

产品定位为“非计量级噪声观察与频谱分析工具”，主要输出：

- 波形；
- dBFS；
- RMS 和数字峰值；
- 实时 FFT；
- 主峰频率；
- 频谱峰值保持；
- iOS 参考估算 dB SPL/dB(A)。

### 4.2 目标用户

- 声学、电子、机械相关学生和教师；
- 家电、风机、电机、车辆等设备的现场运维人员；
- 需要记录异响的售后和质量人员；
- 进行前后对比测试的研发人员；
- 希望了解日常环境噪声的普通用户。

### 4.3 典型场景

- 观察风扇、电机或压缩机的基频和谐波；
- 对比设备维修前后的频谱；
- 记录间歇性异响并回放；
- 使用耳机麦克风靠近狭小或远离手机的位置采集；
- 对比左耳和右耳的两次顺序测量结果；
- 在 iPhone 上查看参考估算声压级。

## 五、双小程序建设方案

### 5.1 产品形态

建设两个独立 AppID：

- 噪声分析测试 iOS 版；
- 噪声分析测试 Android 版。

两个小程序在启动时检查平台：

- iOS 版在非 iOS 设备上提示使用 Android 版；
- Android 版在非 Android 设备上提示使用 iOS 版；
- 不在错误平台启动正式测量流程。

### 5.2 代码组织

建议使用同一代码仓库和共享核心：

```text
共享层
├── DSP/FFT
├── PCM解析
├── 录音状态机
├── 文件与元数据
├── Canvas图表
└── 通用UI组件

平台层
├── iOS音源策略与型号数据库
└── Android音源策略与兼容白名单

构建目标
├── iOS小程序AppID
└── Android小程序AppID
```

这样既满足两个独立小程序的产品要求，也减少算法和页面重复维护。

### 5.3 平台差异

| 项目 | iOS 版 | Android 版 |
|---|---|---|
| 手机音源 | 优先 `buildInMic` | `mic` 或经验证的逻辑音源 |
| 耳机音源 | `auto`/可用耳机音源 | `auto`/`mic`，按兼容表选择 |
| 左右耳 | 提供系统设置引导 | 仅在厂商能力明确时提供引导 |
| 默认单位 | dBFS + 可选参考 SPL | dBFS |
| 型号数据库 | iPhone/AirPods | 仅兼容信息，不存校准值 |
| 未知路由 | 提示并执行输入自检 | 提示并执行输入自检 |

## 六、功能需求

### 6.1 首页与设备检查

首页展示：

- 当前平台；
- 手机型号；
- 微信版本和基础库版本；
- 麦克风权限状态；
- 当前请求的采集模式；
- 耳机连接操作说明；
- 最近一次测试记录；
- “开始分析”入口。

首次启动完成：

1. 隐私说明；
2. 麦克风用途告知；
3. 用户主动授权；
4. 音频输入能力检测；
5. PCM 实时分片自检；
6. 保存设备兼容状态。

### 6.2 采集模式

#### 手机麦克风模式

- 单通道；
- iOS 优先请求 `buildInMic`；
- Android 在无耳机连接时优先使用 `mic`；
- 耳机已连接且无法保证手机输入时，提示用户断开耳机；
- 显示输入源状态和自检结果。

#### 耳机麦克风模式

- 单通道；
- 开始前提示用户确认耳机已连接；
- 请求系统可用的耳机/自动音源；
- 通过引导式敲击测试判断疑似输入位置；
- 耳机断开后立即停止，不自动切换到手机；
- 无法确认输入时显示“耳机输入未验证”，由用户决定是否继续。

### 6.3 左右耳条件性选择

#### iOS/AirPods

Apple 的系统设置允许用户将 AirPods 麦克风设为“自动”“始终左耳”或“始终右耳”，但微信小程序没有对应控制接口。因此，小程序采用引导方案：

1. 用户选择“自动、左耳、右耳”；
2. 若选择左耳或右耳，小程序展示进入 iOS AirPods 设置的图文步骤；
3. 用户完成系统设置后返回小程序；
4. 进行耳机柄附近的敲击/摩擦输入测试；
5. 用户确认后开始测量；
6. 测量记录中保存“用户声明耳侧”和“验证状态”。

界面不得显示“系统已确认左耳/右耳”，除非未来微信提供可信的路由侧信息。当前可显示：

- 自动选择；
- 用户设定左耳；
- 用户设定右耳；
- 耳侧未验证。

#### Android/TWS

Android 耳机品牌和配套应用差异较大。首版策略：

- 默认只提供“自动”；
- 若已验证某品牌/型号支持固定左耳或右耳，在兼容数据库中开放引导；
- 不通过耳机名称推断必然支持左右选择；
- 用户选择的耳侧只作为测量元数据，不声称小程序已控制硬件。

#### 左右耳对比

允许用户分别完成两次单通道录音并进行结果叠加：

- 左耳测试记录 A；
- 右耳测试记录 B；
- 显示两条频谱及频点幅度差；
- 明确标注“非同步顺序测量”；
- 不计算相位差、相干性或声源方向。

### 6.4 模式切换状态机

```text
空闲
→ 请求权限
→ 枚举音源
→ 请求目标模式
→ 启动录音
→ 输入自检
→ 正式分析
→ 暂停/停止
→ 文件封装
→ 回放或保存
```

切换规则：

- 录音中不能直接切换模式；
- 切换必须停止并完成当前文件；
- 切换后清空 FFT 环形缓冲区；
- 每段记录只允许一种输入模式；
- 回放和录音不能同时进行；
- 页面隐藏、来电、微信通话或系统占用时暂停/停止并记录原因。

### 6.5 实时分析

实时页面包括：

- 单通道时域波形；
- FFT 幅度谱；
- 线性/对数频率轴；
- 当前 RMS；
- 数字峰值；
- 削波指示；
- 主峰频率；
- 前 3～5 个显著频率峰；
- 峰值保持；
- 指数平滑；
- 频率范围缩放；
- 采样率和 FFT 点数；
- 当前输入模式和耳侧声明；
- dBFS 或 iOS 参考估算 SPL。

建议默认参数：

| 参数 | 默认值 |
|---|---:|
| 采样率 | 48000 Hz，失败时降级 |
| 输入通道 | 1 |
| PCM | 以真机验证结果为准 |
| FFT 点数 | 4096 |
| 窗函数 | Hann |
| 重叠率 | 50% |
| 频率分辨率 | 48 kHz 时约 11.72 Hz |
| 时间窗 | 48 kHz 时约 85.3 ms |
| UI 刷新率 | 15～20 FPS |
| 幅度范围 | -100～0 dBFS |
| 默认频率轴 | 对数 |

### 6.6 录音与回放

- 开始、暂停、继续、停止；
- 单次录音不超过微信接口上限；
- 实时 PCM 同时进入分析管线和本地写入管线；
- 停止后封装为 WAV 或生成可播放文件；
- 录音命名、备注、标签；
- 播放、暂停、跳转、进度显示；
- 删除和批量清理；
- 离线重新计算频谱；
- 导出音频、频谱截图和 JSON/CSV 分析结果；
- 可选云端备份，默认仅本地保存。

### 6.7 历史记录与对比

每条记录保存：

- 记录 ID；
- 创建时间和时区；
- 平台和手机型号；
- 用户选择的耳机型号；
- 手机/耳机模式；
- 自动/左耳/右耳声明；
- 耳侧验证状态；
- 请求音源；
- 采样率、通道数、FFT 参数；
- 原始单位；
- iOS 参考修正配置版本；
- 录音文件路径；
- RMS、峰值、主峰等摘要；
- 中断和兼容性警告。

支持在相同参数下选择两条记录对比。参数不一致时允许查看，但给出不可直接比较提示。

## 七、技术架构

```text
RecorderManager（单通道 PCM）
        │
        ├── 实时帧管线
        │      ├── PCM格式探测/校验
        │      ├── DC去除
        │      ├── 环形缓冲
        │      ├── Hann加窗
        │      ├── FFT/PSD
        │      ├── RMS/Peak/Clipping
        │      └── 平滑和峰值检测
        │
        ├── 本地录音管线
        │      ├── 顺序写入
        │      ├── WAV封装
        │      └── 元数据保存
        │
        └── 状态与异常
               ├── 权限
               ├── 系统中断
               ├── 耳机断连
               └── 格式/性能降级

DSP Worker ──> 低密度显示数据 ──> Canvas
WAV文件 ─────────────────────> InnerAudioContext
```

### 7.1 主要模块

1. `CaptureService`：权限、录音器生命周期和实时帧。
2. `InputModeController`：手机/耳机互斥、平台音源策略。
3. `EarSideGuide`：左右耳设置指引、用户声明和输入自检。
4. `PCMParser`：格式、字节序、帧对齐和异常数据校验。
5. `DSPWorker`：FFT、RMS、峰值、滤波和后续算法。
6. `SpectrumRenderer`：波形和频谱 Canvas 绘制。
7. `RecordingRepository`：音频文件和元数据生命周期。
8. `PlaybackService`：回放及播放中断。
9. `IOSReferenceSPLService`：型号参考修正、可信度和版本管理。
10. `CompatibilityService`：设备、耳机和微信版本兼容规则。

### 7.2 实时性能原则

- FFT 在 Worker 中运行；
- 页面线程仅接收已压缩的绘图数组；
- 文件采用流式写入，不累计完整 PCM 于内存；
- 根据性能等级将 FFT 从 8192/4096 降至 2048；
- 绘图帧率与音频计算帧率解耦；
- 页面不可见时停止高频绘图；
- 记录丢帧数量和最长处理时间。

## 八、iOS 参考估算声压级

### 8.1 输出原则

iOS 始终保留 dBFS 原始结果。在型号数据可信时额外提供：

- 参考估算 dB SPL；
- 参考估算 dB(A)；
- 数据来源和可信等级；
- 明显的“非校准”标识。

禁止使用“精确声压”“专业声级计”“已校准”等表述。

### 8.2 换算模型

基础模型：

```text
参考估算声压级 = 数字RMS(dBFS) + 型号/路由/模式参考偏移
```

有可靠频响数据时：

```text
参考频谱(f) = dBFS频谱(f) + 宽带偏移 + 频率修正曲线(f)
```

### 8.3 数据库维度

每个修正配置至少包含：

- iPhone 型号；
- iOS 大版本；
- 输入模式；
- 手机或耳机；
- AirPods 型号；
- 自动/左耳/右耳；
- 采样率；
- 录音格式；
- 参考偏移；
- 频率修正曲线；
- 有效声级和频率范围；
- 数据来源；
- 样本数量；
- 可信等级；
- 配置版本和发布日期。

### 8.4 回退规则

以下情况只显示 dBFS：

- 无同型号数据；
- 手机型号为 unknown；
- AirPods 型号由用户填写但无匹配数据；
- 输入模式或采样率与数据源不一致；
- 耳机输入未通过自检；
- 数据仅来自近似型号；
- 当前处理链可能发生未知变化。

### 8.5 建议可信等级

- A：同型号、同路由、同参数的明确实验数据；
- B：同型号但系统或录音模式不完全一致，只显示范围；
- C：相邻型号或来源不完整，不输出数值 SPL；
- U：未知，只显示 dBFS。

## 九、Android 测量策略

Android 首期统一输出：

- dBFS；
- RMS；
- Peak；
- FFT 和 PSD；
- 同设备、同输入模式下的相对差值。

不输出型号估算 SPL。兼容数据库只记录：

- 手机型号；
- Android/厂商系统版本；
- 微信版本；
- PCM 是否稳定；
- 实际可用采样率；
- 耳机模式是否通过测试；
- 是否出现自动增益或明显降噪；
- 支持的耳机型号；
- 是否可引导固定左耳/右耳。

## 十、后续算法扩展

在单通道 DSP 接口上逐步增加：

- 声谱图；
- 1/1 和 1/3 倍频程；
- A/C/Z 数字计权；
- Leq、Lmax、Lmin；
- L10、L50、L90；
- 谐波与 THD；
- 窄带峰值追踪；
- 异响事件检测；
- 两次顺序测量的频谱差；
- 样本标签和设备故障分类。

未获得可靠 SPL 修正时，所有加权和统计指标仍以相对数字量表达。

## 十一、非功能指标

| 指标 | MVP 验收目标 |
|---|---|
| 实时频谱延迟 | 中端目标机 P95 ≤ 300 ms |
| UI 刷新率 | ≥ 15 FPS |
| FFT 计算 | 4096 点 P95 ≤ 20 ms |
| 频率准确度 | 1 kHz 测试音峰值误差不超过一个 bin |
| 稳定性 | 前台连续分析 30 分钟不崩溃 |
| 文件内存 | 录音期间不随时长线性占用运行内存 |
| 回放时长 | 与录音时长误差 ≤ 100 ms，分段边界除外 |
| 模式互斥 | 手机/耳机不存在两个并行录音实例 |
| 耳机断连 | 2 秒内停止并明确提示 |
| 数据诚实性 | 未验证路由不得显示“已确认耳机/左耳/右耳” |

## 十二、测试计划

### 12.1 技术 PoC

优先验证：

- PCM 分片格式、位深、字节序和连续性；
- 48/44.1/32/16 kHz 实际行为；
- 手机和耳机音源切换；
- AirPods 和主流 TWS 是否实际启用麦克风；
- 耳机断开行为；
- 左右耳系统设置引导是否可用；
- FFT 延迟、掉帧和发热；
- PCM 录音转 WAV 与回放；
- 来电、微信通话、页面隐藏和锁屏行为。

### 12.2 设备矩阵

iOS 建议至少：

- 4 个不同代际 iPhone；
- 2 个不同代际 AirPods；
- 2 个非 Apple TWS；
- 1 个有线耳机。

Android 建议至少：

- 华为、小米、OPPO、vivo、荣耀、三星等共 8～12 台；
- Android 不同大版本和厂商系统；
- 5 个主流 TWS 品牌；
- 1 个有线耳机。

### 12.3 声学信号测试

- 100 Hz、1 kHz、5 kHz、10 kHz 单音；
- 双音和多音；
- 白噪声、粉红噪声；
- 扫频；
- 不同声级；
- 瞬态敲击；
- 手机与耳机顺序测量对比；
- 左耳和右耳顺序测量对比。

### 12.4 兼容等级

- L1：手机和耳机均通过，PCM、FFT、录音完整；
- L2：手机通过，耳机输入无法确认；
- L3：只能使用降级采样率或压缩分片；
- L4：实时分析不支持，只允许普通录音；
- 禁用：存在数据损坏或严重不一致。

## 十三、隐私与合规

- 默认本地分析和本地保存；
- 用户主动点击后才申请麦克风权限；
- 录音期间始终显示明显状态；
- 原始 PCM 不写入普通日志；
- 上传录音须单独确认；
- 隐私政策说明采集目的、保存位置、保存期限和删除方式；
- 用户可以删除全部本地和云端记录；
- 云端传输采用 HTTPS 和鉴权；
- iOS 估算 SPL 页面必须展示非校准免责声明；
- 不将结果用于职业健康或执法判断。

## 十四、主要风险

| 风险 | 等级 | 应对 |
|---|---:|---|
| 两个小程序仍受微信 API 限制 | 高 | PoC 先验证，不把拆分平台等同于原生权限 |
| AirPods 实际路由不可读取 | 高 | 输入自检、用户确认、保守状态文案 |
| 小程序不能设置 AirPods 左右耳 | 高 | 系统设置引导，仅保存用户声明 |
| Android TWS 厂商差异 | 高 | 白名单、兼容等级和自动回退 |
| 型号参考数据不足 | 高 | 无数据只显示 dBFS，不使用近似型号硬换算 |
| AGC/降噪改变频谱 | 高 | 相对分析定位、真机特征测试、警告标签 |
| 长录音内存增长 | 高 | 流式落盘、限制单段时长 |
| 实时 FFT 卡顿 | 中 | Worker、降采样绘图、性能分级 |
| 模式切换产生残留数据 | 中 | 严格状态机并重置全部缓冲区 |
| 用户错误选择耳机型号/耳侧 | 中 | 二次确认并在记录中标明“用户声明” |

## 十五、项目阶段与工期

### 阶段 0：技术预研，2～3 周

交付：

- iOS/Android PCM 采集 PoC；
- 手机/耳机模式验证；
- AirPods/TWS 测试表；
- 左右耳引导验证；
- 实时 FFT 演示；
- WAV 录音回放；
- Go/No-Go 报告。

### 阶段 1：共享核心与 iOS MVP，4～5 周

- 单通道 DSP Worker；
- 实时波形和 FFT；
- 录音、WAV、回放；
- 手机/耳机模式；
- 左右耳引导；
- iPhone 型号数据库框架；
- 参考 SPL 展示和回退规则。

### 阶段 2：Android MVP，3～4 周

- Android 音源策略；
- dBFS 和相对频谱；
- TWS 兼容白名单；
- 设备性能降级；
- Android 专属异常处理。

### 阶段 3：联合测试与发布，3～4 周

- 设备矩阵测试；
- 性能、发热和稳定性优化；
- 隐私与审核材料；
- 灰度发布；
- 兼容表和用户帮助文档。

总体预计 12～16 周。

### 人员建议

- 小程序工程师：2 人；
- DSP/音频工程师：0.5～1 人；
- 测试工程师：1 人；
- 产品经理：0.5 人；
- UI/UX：0.5 人；
- 后端工程师：仅云同步阶段需要 0.5～1 人。

预计基础 MVP 为 32～44 人周；加入云同步、报告后台和批量设备管理后增加约 8～12 人周。

## 十六、MVP 验收标准

### 核心功能

- 两个小程序分别在目标平台运行；
- 手机单通道录音和 FFT 正常；
- 目标白名单设备的耳机单通道录音和 FFT 正常；
- 手机/耳机切换不会产生并行采集；
- 耳机断开能够安全停止；
- PCM 可生成有效 WAV 并回放；
- 历史记录能够查看、删除和离线分析；
- 左右耳选择明确标识为用户设置/声明；
- 不出现双通道或同步双耳分析入口。

### iOS 参考 SPL

- 有 A 级数据时显示参考估算值、来源和版本；
- 无同型号可靠数据时只显示 dBFS；
- AirPods 输入未验证时不显示参考 SPL；
- 所有估算页面显示非校准声明；
- 不作专业精度和标准符合性承诺。

### 性能与稳定性

- 目标机型频谱刷新率和延迟达标；
- 连续分析不发生内存持续增长；
- 来电、微信通话和页面切换后状态正确；
- 所有异常录音均有明确结束状态，不生成被误认为完整的数据。

## 十七、后续版本建议

### V1.1

- 声谱图；
- 左/右耳顺序测量模板；
- CSV/JSON 报告；
- 更多 iPhone 参考配置；
- 设备兼容反馈入口。

### V1.2

- 1/3 倍频程；
- 参考 Leq、Lmax、L90；
- 设备前后对比报告；
- 云端同步和分享。

### V2.0

- 异响事件检测；
- 设备类型模板；
- 基于已标注录音的故障分类；
- 外部校准麦克风或专用硬件接入评估。

## 十八、立项建议

建议批准阶段 0 技术预研，暂不直接承诺完整上线范围。PoC 必须回答：

1. 目标 iPhone 与 Android 设备能否稳定返回可解析的单通道 PCM；
2. 目标 AirPods/TWS 是否实际使用耳机麦克风；
3. 手机/耳机模式切换失败时能否被用户识别；
4. 左右耳设置引导在目标耳机上是否可执行；
5. 实时 FFT 是否满足延迟和稳定性要求；
6. iOS 公开参考数据是否足以覆盖首批型号。

阶段 0 通过后再冻结 MVP 型号白名单、iOS 参考 SPL 覆盖范围和正式开发排期。

## 十九、主要技术依据

- [微信官方 RecorderManager 启动参数定义](https://github.com/wechat-miniprogram/api-typings/blob/master/types/wx/lib.wx.api.d.ts#L10323-L10386)：音源、单/双通道、PCM、采样率和录音时长。
- [微信官方音源枚举定义](https://github.com/wechat-miniprogram/api-typings/blob/master/types/wx/lib.wx.api.d.ts#L5305-L5332)：iOS 与 Android 可请求的逻辑音源。
- [微信官方实时录音帧定义](https://github.com/wechat-miniprogram/api-typings/blob/master/types/wx/lib.wx.api.d.ts#L8790-L8795)：实时 `ArrayBuffer` 分片。
- [微信官方录音停止结果](https://github.com/wechat-miniprogram/api-typings/blob/master/types/wx/lib.wx.api.d.ts#L8990-L8996)：临时文件、时长和文件大小。
- [Apple AirPods 麦克风左右设置](https://support.apple.com/en-gb/108764)：自动、始终左耳或始终右耳。
- [Apple 高质量蓝牙录音](https://developer.apple.com/documentation/avfaudio/avaudiosession/categoryoptions-swift.struct/bluetoothhighqualityrecording)：仅用于说明原生系统能力；微信小程序未暴露该配置。
- [Android MediaRecorder.AudioSource](https://developer.android.com/reference/android/media/MediaRecorder.AudioSource)：Android 逻辑录音源及其可能的信号处理。

