# 噪声分析测试小程序（MVP）

原生微信小程序初版，按策划书中的“单通道、手机/耳机互斥、以 dBFS 为主”的范围实现。

跨电脑继续开发时，请先阅读 [`DEVELOPMENT_HANDOFF.md`](./DEVELOPMENT_HANDOFF.md)。

## 启动

使用微信开发者工具导入本文件夹。当前 `project.config.json` 已配置 AppID `wx4e4e4dd771fc9421`。iOS 为首轮验证平台，Android 使用标准麦克风兼容模式，仍需补充机型测试。

`project.ios.config.example.json` 和 `project.android.config.example.json` 保留为不同测试环境的配置模板；正式发布使用当前 `project.config.json`。

## 已实现

- 首页设备/权限状态、手机与耳机单通道互斥选择
- PCM 实时帧解析、连续 7 Hz 高通、129 抽头 FIR 抗混叠低通、48 kHz 至 4 kHz 降采样、2048 样本 Hann 窗并零填充至 4096 点 FFT（0.98 Hz/bin、有效分辨率约 1.95 Hz）、RMS、数字峰值、削波检测
- 20 Hz 至可调上限的频谱绘图，上限可在 100–1000 Hz 间设置；频谱支持线性/对数频率轴、A/线性计权、带数值的刻度尺、虚线网格，以及 dBA/dB 自动或手动纵轴范围
- 可将当前频谱设为会话内参考曲线，以橙色虚线与紫色实时曲线叠加显示；20–60 Hz、70–150 Hz、160–240 Hz 使用不同的半透明频带背景
- 可从本地 WAV 录音列表导入参考：分块读取文件，以 512 ms 窗和 50% 重叠计算功率平均 FFT，并作为文件参考曲线显示
- 实时和参考频谱分别显示总声压级，以及低频（20–60 Hz）、中频（70–150 Hz）、空腔声（160–240 Hz）三个频带峰值；切换计权时曲线与全部指标同步重算
- 频谱约 20–24 FPS 刷新，当前曲线和指标使用功率域滑动平均；默认覆盖过去 3 秒，可在 1–30 秒间调整。算法只保留最长 30 秒的紧凑缓存并维护增量和；绘图请求合并、按屏幕像素合并绘图点，FFT 旋转因子、Hann 窗及音频/DSP 工作缓冲区均复用
- 频谱默认使用固定 `-80～-20 dB` 纵轴；计权、平均时长、横纵坐标及参考曲线操作统一收纳在图窗下方的“绘图设置”中
- FFT 幅值按 Hann 窗相干增益归一化，避免使用矩形窗尺度造成约 `-6.02 dB` 的系统性低估；FFT 输入长度不是 2 的幂时会明确拒绝
- 频谱画布按手机卡片宽度和可用屏幕高度自适应，坐标字号与绘图区边距同步缩放
- 暂停时先更新交互状态并立即跳过后续 PCM 计算，暂停/继续按钮使用不同状态色
- 分析与录音分离：仅在用户点击“开始录音”后保存音频，每段最多 30 秒；录音时显示 0.1 秒精度的时长与进度，结束后以分块方式将 PCM 自动封装为可回放的 WAV
- 录音保存成功后停留在实时分析页、显示成功提示并自动恢复实时分析，不再自动跳转详情；若因用户结束分析或输入中断而保存则不自动恢复
- 独立“数据对比”页可勾选最多 8 个录音，在响应式图窗中叠加频谱，并按曲线颜色列出总声压级；计权、横轴和纵轴范围可调
- 历史元数据、本地详情、顺序测量频谱叠加
- 独立“录音文件”标签页，集中显示文件格式、时长、大小、采集模式并进入回放详情；支持列表内重命名，以及左滑置顶、取消置顶和删除
- 记录详情支持本地重命名、备注和标签编辑；旧记录无需迁移即可继续查看和补充信息
- 耳机左右侧用户声明和未验证状态文案
- iOS 耳机模式锁定 `headsetMic`，并通过输入源状态、非预期暂停和音频中断守护阻止断开后回退到手机麦克风
- 无可信型号配置时不显示 iOS SPL；Android 始终只显示 dBFS

## 真机 PoC 待确认

- 微信上返回 PCM 的实际位深、字节序、采样率与 `audioSource` 路由
- 耳机断开事件和 AirPods/TWS 实际输入位置
- iPhone 与 AirPods 真机生成 WAV 后的回放、时长与文件完整性
- 每个目标设备的 FFT 延迟、丢帧与发热数据

本产品不作为经校准声级计使用。

手机型号可以用于选择参考修正配置，但不能仅靠检索“麦克风灵敏度”得到真实 SPL：同型号个体差异、麦克风方向、保护壳、输入路由以及微信/iOS 的增益和信号处理都会改变结果。绝对声压级应使用同一设备、同一路由与标准声源或已校准声级计进行现场校准；没有可信配置时继续标为估计值。

## 首轮已确定设备

| 项目 | 首测设备 | 需要现场记录的补充信息 |
| --- | --- | --- |
| 手机 | iPhone 16 Pro | iOS 版本、微信版本、基础库版本 |
| 无线耳机 | AirPods Pro（型号 A3065） | 左右耳与充电盒的完整型号、固件版本、系统“麦克风”设置 |

Apple 将 A3063、A3064、A3065 归类为 AirPods Pro 3 系列。因此首轮测试记录会将 A3065 作为 AirPods Pro 3 兼容样本，而不是假定为 AirPods Pro 2。该信息只用于兼容性记录，不用于声压级校准。

首轮运行基线：iPhone 16 Pro、iOS 26.6.1、微信 8.0.76。AirPods 固件版本尚未记录，因此耳机采集默认视为“未验证”，且不显示参考 SPL。
